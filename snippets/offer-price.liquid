{% if product.compare_at_price_max > product.price %}<span class="offer-price">{{ 'products.product.save' |t }} &nbsp;<b>{{ product.compare_at_price_max | minus:product.price | times:100 | divided_by:product.compare_at_price_max }}% </b></span>{% endif %}