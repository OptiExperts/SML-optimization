

  <div class="grid__item shortcodes-section">
      <!--theme-toggle-frame-set starts-->
      <div class="theme-toggle-frame-set">
        {% for block in section.blocks %}          
          <h5 class="theme-toggle-accordion  {% if forloop.first == true %}active{% endif %}"><a href="#">{{ block.settings.title }}</a></h5>
          <div class="theme-toggle-content {% if forloop.last %} last{% endif %}" {% if forloop.first == true %}style="display:block;"{% endif %}>
            <div class="theme-block">
              {{ block.settings.description }}
            </div>
          </div>        
        {% endfor %}           
    </div>     
      <!--theme-toggle-frame-set ends-->

</div>
<div class="space-clear-hidden-medium"></div>




{% schema %}
{
	"name": "FAQs",
	"max_blocks": 50,
	"settings": [],
	"blocks" : [
		{
			"type": "text",
			"name": "FAQ",
			"settings": [
				{
					"type": "text",
					"id": "title",
					"default": "Question",
					"label": "Question"
				},
				{
					"type": "textarea",
					"id": "description",
					"default": "Answer goes here...",
					"label": "Text"
				}
			]
		}
	]	
}
{% endschema %}
